import 'package:flutter/material.dart';
import 'package:styled_widget/styled_widget.dart';

final emailNotificationsSettingsPageEmailContainer = (
  Widget child,
) =>
    Styled.widget(
      child: child,
    ).padding(
      bottom: 16,
    );
